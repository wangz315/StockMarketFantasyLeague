<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comp4350b.com.stockmarketfantasyleague">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@android:mipmap/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">
        <activity
            android:name=".Presentation.Activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Presentation.Activities.LoginActivity" />
        <activity android:name=".Presentation.Activities.RegisterActivity" />
        <activity android:name=".Presentation.Activities.UserHomeActivity"
            android:launchMode="singleTop"/>
        <activity
            android:name=".Presentation.Activities.ViewStockActivity"
            android:label="@string/title_activity_view_stock"
            android:parentActivityName=".Presentation.Activities.UserHomeActivity"
            android:theme="@style/Theme.Design.NoActionBar">
        </activity>
        <activity android:name=".Presentation.Activities.BuyStockActivity" />
        <activity android:name=".Presentation.Activities.SellStockActivity"/>
        
    </application>

    
</manifest>